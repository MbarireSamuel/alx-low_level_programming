#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char s;

	int m;

	s = 'a';
	m = 0;
	while
		(m < 10) {
			putchar(m + '0');
			m++;
		}
	while
		(s <= 'f') {
			putchar(s);
			s++;
		}
	putchar('\n');
	return (0);
}
