#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int s;
	s = 0;
	while
		(s < 10) {
			putchar(s + '0');
			s++;
		}
	putchar('\n');
	return (0);
}
