#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char s;

	s = 'z';
	while
		(s >= 'a') {
			putchar(s);
			s--;
		}
	putchar('\n');
	return (0);
}
