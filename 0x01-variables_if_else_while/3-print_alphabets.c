#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (success)
 */

int main(void)
{
	char s;

	char m;

	s = 'a';
	m = 'A';
	while
		(s <= 'z') {
			putchar(s);
			s++;
		}
	while
		(m <= 'Z') {
			putchar(m);
			m++;
		}
	putchar('\n');
	return (0);
}
